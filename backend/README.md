# SignAI Platform Backend

手语AI智能平台后端服务

## 快速启动

```bash
# 安装依赖
pip install -r requirements.txt

# 启动服务
cd backend
python main.py
```

## API 文档

启动后访问 http://localhost:8000/docs 查看完整的 API 文档

## 环境变量

参考根目录的 `.env` 文件配置环境变量